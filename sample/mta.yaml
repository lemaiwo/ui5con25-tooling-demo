_schema-version: "3.2"
ID: toolingsample
description: Generated by migration tool of Wouter Lemaire
version: 0.0.1
modules:
- name: toolingsample-dest-content
  type: com.sap.application.content
  requires:
  - name: dest-srv
    parameters:
      content-target: true
  - name: toolingsample-repo-host
    parameters:
      service-key:
        name: toolingsample-repo-host-key
  - name: toolingsample-uaa
    parameters:
      service-key:
        name: toolingsample-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: toolingsample_repo_host
          ServiceInstanceName: toolingsample-html5-srv
          ServiceKeyName: toolingsample-repo-host-key
          sap.cloud.service: toolingsample
        - Authentication: OAuth2UserTokenExchange
          Name: toolingsample_uaa
          ServiceInstanceName: toolingsample-xsuaa-srv
          ServiceKeyName: toolingsample-uaa-key
          sap.cloud.service: toolingsample
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: toolingsample-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: toolingsample-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - toolingsample.zip
      name: toolingsample
      target-path: resources/
- name: toolingsample
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build-cf
    supported-platforms: []
resources:
- name: dest-srv
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: dest-srv
    service-plan: lite
- name: toolingsample-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: toolingsample-xsuaa-srv
    service-plan: application
- name: toolingsample-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: toolingsample-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true